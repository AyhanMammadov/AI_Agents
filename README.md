# Stateless Auth Service

## Endpoints

- POST /login {"username": "demo", "password": "password"} -> {"token": "..."}

## Run

1. Install dependencies:
   pip install -r requirements.txt
2. Start server:
   python main.py

## Notes
- Default user: demo / password
